---
phase: 05-dashboard-landing-page-and-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/page.tsx
  - components/landing/hero-section.tsx
  - components/landing/curriculum-overview.tsx
  - components/landing/features-section.tsx
  - components/landing/cta-section.tsx
  - app/globals.css
  - components/content/code-diff.tsx
  - components/layout/app-sidebar.tsx
  - app/(academy)/layout.tsx
autonomous: true
requirements: [PLAT-02, DSGN-03, DSGN-04]

must_haves:
  truths:
    - "Landing page at / renders hero section with animated gradient background, Zama dark theme with gold/purple accents"
    - "Landing page shows 4-week curriculum overview with week titles, goals, and lesson counts"
    - "Landing page has features section showcasing code comparisons, quizzes, AI Grader, and progress tracking"
    - "Landing page has a single 'Start Learning' CTA button linking to /week/1/lesson/why-privacy-matters"
    - "Landing page renders full-width without sidebar (outside academy layout)"
    - "Landing page is fully responsive: stacks vertically on mobile, grid on desktop"
    - "CodeDiff component stacks vertically on mobile (already has grid-cols-1 md:grid-cols-2)"
    - "Mobile sidebar uses sheet overlay with hamburger trigger (SidebarTrigger present in mobile header, Sheet opens slide-over)"
  artifacts:
    - path: "app/page.tsx"
      provides: "Landing page composition"
      min_lines: 20
    - path: "components/landing/hero-section.tsx"
      provides: "Hero with animated gradient"
      min_lines: 25
    - path: "components/landing/curriculum-overview.tsx"
      provides: "4-week curriculum grid"
      min_lines: 30
    - path: "components/landing/features-section.tsx"
      provides: "Features showcase with visual FHE concepts"
      min_lines: 30
    - path: "components/landing/cta-section.tsx"
      provides: "Final call to action"
      min_lines: 15
  key_links:
    - from: "app/page.tsx"
      to: "components/landing/*.tsx"
      via: "import and compose"
      pattern: "import.*landing"
    - from: "components/landing/curriculum-overview.tsx"
      to: "lib/curriculum.ts"
      via: "import curriculum data"
      pattern: "import.*curriculum"
---

<objective>
Build the polished landing page at / with hero section, curriculum overview, features showcase, and CTA. Add responsive design fixes. The landing page serves dual purpose: jury impression for the Zama Bounty Track AND student onboarding.

Purpose: First impression for judges and students. This is the highest-jury-impact item.
Output: Complete landing page with 4 sections, animated gradient hero, responsive layout.
</objective>

<execution_context>
@/Users/0xemrek/.claude/get-shit-done/workflows/execute-plan.md
@/Users/0xemrek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dashboard-landing-page-and-polish/05-CONTEXT.md
@.planning/phases/05-dashboard-landing-page-and-polish/05-RESEARCH.md

@app/page.tsx
@app/layout.tsx
@lib/curriculum.ts
@app/globals.css
@components/content/code-diff.tsx

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From lib/curriculum.ts:
```typescript
export interface Lesson {
  id: string; slug: string; title: string; type: "conceptual" | "hands-on"
}
export interface Homework {
  slug: string; title: string; description: string;
  deliverables: string[]; difficulty: "beginner" | "intermediate" | "advanced"
}
export interface Week {
  id: number; title: string; goal: string;
  learningObjectives: string[]; lessons: Lesson[]; homework: Homework
}
export const curriculum: Week[] // 4 weeks
```

From app/layout.tsx:
```typescript
// Root layout wraps with ThirdwebProviderWrapper only — no sidebar
// Landing page at app/page.tsx renders with just this root layout
```

From app/globals.css (key color variables):
```css
/* Zama brand colors available as CSS custom properties */
--background, --primary (purple), --primary-foreground, --text-primary, --text-secondary, --text-muted, --border, --surface, --card
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create landing page sections and animated gradient</name>
  <files>
    components/landing/hero-section.tsx,
    components/landing/curriculum-overview.tsx,
    components/landing/features-section.tsx,
    components/landing/cta-section.tsx,
    app/globals.css
  </files>
  <action>
Create 4 landing page section components in `components/landing/`:

**hero-section.tsx** (Server component — NO "use client"):
- Full-height hero (min-h-[80vh]) with animated gradient background
- Add CSS keyframe animation to app/globals.css: `@keyframes gradient-shift` that shifts background-position over 15s
- Background: linear-gradient with dark purple/indigo hints (#0A0A0F, #1A1035, #1A1A24) — subtle, not distracting
- Content: uppercase subtitle "4-Week FHEVM Developer Bootcamp", bold headline "Learn to Build **Confidential** Smart Contracts" (primary color on "Confidential"), descriptive paragraph, "Start Learning" button linking to `/week/1/lesson/why-privacy-matters`
- Responsive: text-5xl sm:text-6xl lg:text-7xl for headline, max-w-4xl centered
- Use Zama brand colors from existing CSS custom properties

**curriculum-overview.tsx** (Server component):
- Import `curriculum` from `@/lib/curriculum`
- Section title "Curriculum" with brief intro
- 4-card grid: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6`
- Each card: week number badge, week title, week goal, lesson count + homework indicator
- Cards styled with border, bg-card, rounded-xl, hover state
- Use lucide icons (BookOpen, Code, FileCode, etc.) for visual variety per week

**features-section.tsx** (Server component):
- Section title "Platform Features" or "Built for Learning"
- 2x2 or 3-column grid showcasing key differentiators:
  1. Side-by-Side Code Comparisons (CodeDiff visual — icon + description)
  2. Interactive Quizzes (quiz icon + description)
  3. AI-Powered Grading (Bot icon + description)
  4. Progress Tracking (chart icon + description)
- Each feature: icon, title, brief description (2-3 sentences)
- This section fulfills DSGN-04 (Visual FHE concept diagrams): the CodeDiff components used in lessons serve as the visual diagram approach (side-by-side Solidity vs FHEVM comparisons). The features section documents this explicitly with a visual example or description of the code comparison pattern. Additionally, include a simple visual FHE flow element (e.g., a 3-step diagram: "Encrypt -> Compute on Encrypted Data -> Decrypt") using styled divs/icons to make the FHE concept tangible on the landing page

**cta-section.tsx** (Server component):
- Bottom CTA with dark gradient background
- Heading: "Ready to Build Confidential dApps?"
- Brief persuasive text + "Start Learning" button (same link as hero)
- Subtle border-top separator

**app/globals.css** additions:
- Add `.hero-gradient` class with animated gradient background
- `background-size: 400% 400%; animation: gradient-shift 15s ease infinite`
- Keep animation subtle — dark base with purple hints that shift

All components responsive: stack vertically on mobile, grid on larger screens. No "use client" needed — these are pure presentational server components using static data from curriculum.ts.
  </action>
  <verify>
    <automated>cd /Users/0xemrek/Desktop/fheacademy && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>Four landing page section components exist in components/landing/, each renders responsive content with Zama branding. globals.css has hero gradient animation.</done>
</task>

<task type="auto">
  <name>Task 2: Compose landing page and verify responsive layout</name>
  <files>
    app/page.tsx,
    components/layout/app-sidebar.tsx
  </files>
  <action>
**app/page.tsx** — Replace the current minimal placeholder with the full landing page:
- Import and compose all 4 sections: HeroSection, CurriculumOverview, FeaturesSection, CtaSection
- Full-width layout (no sidebar) — this file is OUTSIDE the (academy) route group, so it only gets root layout
- Add a minimal navbar at top: "FHE Academy" text-logo on left, no wallet connect on landing page per user decision
- Add a footer at bottom with "Built for the Zama Bounty Track" or similar branding
- Wrap in a `<div className="min-h-screen bg-background">` container
- IMPORTANT: Do NOT import useProgress, ProgressProvider, or any client hooks — landing page is a pure server component
- Do NOT add a sidebar or import sidebar components

**components/layout/app-sidebar.tsx** — Add Dashboard link:
- In the SidebarHeader, after the "FHE Academy" link and "Syllabus" link, add a "Dashboard" link to `/dashboard`
- Use LayoutDashboard icon from lucide-react
- Same styling pattern as the existing Syllabus link
- This prepares the sidebar for Plan 02's dashboard page
  </action>
  <verify>
    <automated>cd /Users/0xemrek/Desktop/fheacademy && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>Landing page at / renders all 4 sections in a full-width layout without sidebar. Sidebar has Dashboard link. Build succeeds with no errors.</done>
</task>

<task type="auto">
  <name>Task 3: Verify and implement mobile sidebar hamburger/Sheet overlay</name>
  <files>
    components/layout/app-sidebar.tsx,
    app/(academy)/layout.tsx
  </files>
  <action>
Verify and implement the mobile sidebar sheet overlay per CONTEXT.md locked decision: "Mobile sidebar: sheet overlay (hamburger menu opens slide-over sheet)."

**Verify existing implementation:**
1. Check if `SidebarTrigger` from shadcn sidebar is already rendered in the mobile header area (the (academy) layout or a header component)
2. Check if the sidebar uses the shadcn `Sheet` component for mobile overlay behavior
3. Check that at mobile breakpoints (below 768px), the sidebar is hidden by default and accessible via hamburger menu

**If missing, implement:**
- In `app/(academy)/layout.tsx` or the appropriate header component:
  - Ensure a `SidebarTrigger` (hamburger icon button) is visible at mobile breakpoints (`md:hidden`)
  - The trigger should open the sidebar as a `Sheet` overlay (slide-in from left)
  - The sheet overlay should contain the full sidebar navigation
- The shadcn sidebar component (`@/components/ui/sidebar`) already supports sheet mode via `collapsible="icon"` or the `SIDEBAR_KEYBOARD_SHORTCUT` — verify this is wired up
- If the sidebar component uses `SidebarProvider` with `open`/`onOpenChange`, ensure mobile sheet behavior is functional

**Key details:**
- The shadcn sidebar already imports Sheet internally — the key is ensuring SidebarTrigger is present and visible on mobile
- Do NOT hide navigation on mobile — all sidebar links must be accessible via the sheet overlay
- Test by verifying the hamburger trigger element exists in the DOM and is visible below md breakpoint
  </action>
  <verify>
    <automated>cd /Users/0xemrek/Desktop/fheacademy && grep -r "SidebarTrigger" app/ components/layout/ && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>Mobile sidebar hamburger trigger (SidebarTrigger) is present in the mobile header. At mobile breakpoints, tapping the hamburger opens the sidebar as a Sheet overlay. All sidebar navigation links are accessible on mobile. Build succeeds.</done>
</task>

</tasks>

<verification>
1. `npx next build` succeeds with no errors
2. Landing page at `/` renders hero, curriculum overview, features, and CTA sections
3. Landing page has no sidebar (renders outside academy layout)
4. "Start Learning" button links to `/week/1/lesson/why-privacy-matters`
5. Curriculum overview shows all 4 weeks with titles, goals, and lesson counts from curriculum.ts
6. Features section showcases code comparisons, quizzes, AI grading, and progress tracking (fulfills DSGN-04)
7. Features section includes a simple visual FHE flow element (Encrypt -> Compute -> Decrypt)
8. All sections stack vertically on mobile viewports
9. Sidebar has a "Dashboard" link to /dashboard
10. SidebarTrigger (hamburger) is present in mobile header and opens Sheet overlay at mobile breakpoints
</verification>

<success_criteria>
- Landing page is the root (/) with full-width marketing layout, no sidebar
- Hero has animated gradient background with Zama dark theme
- Curriculum overview displays 4-week structure from curriculum.ts data
- Features section showcases platform differentiators (visual FHE concepts per DSGN-04)
- CTA section with "Start Learning" button
- All sections responsive across desktop/tablet/mobile
- CodeDiff already responsive (grid-cols-1 md:grid-cols-2 verified in codebase)
- Mobile sidebar: SidebarTrigger (hamburger) visible at mobile breakpoints, opens Sheet overlay with full sidebar navigation
- Build compiles successfully
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard-landing-page-and-polish/05-01-SUMMARY.md`
</output>
