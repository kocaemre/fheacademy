---
phase: 05-dashboard-landing-page-and-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - hardhat/week-1/starter/hardhat.config.ts
  - hardhat/week-1/starter/package.json
  - hardhat/week-1/starter/contracts/TemperatureConverter.sol
  - hardhat/week-1/starter/test/TemperatureConverter.ts
  - hardhat/week-1/starter/tsconfig.json
  - hardhat/week-1/solution/hardhat.config.ts
  - hardhat/week-1/solution/package.json
  - hardhat/week-1/solution/contracts/FHETemperatureConverter.sol
  - hardhat/week-1/solution/test/FHETemperatureConverter.ts
  - hardhat/week-1/solution/tsconfig.json
  - hardhat/week-2/starter/hardhat.config.ts
  - hardhat/week-2/starter/package.json
  - hardhat/week-2/starter/contracts/ConfidentialERC20.sol
  - hardhat/week-2/starter/test/ConfidentialERC20.ts
  - hardhat/week-2/starter/tsconfig.json
  - hardhat/week-2/solution/hardhat.config.ts
  - hardhat/week-2/solution/package.json
  - hardhat/week-2/solution/contracts/ConfidentialERC20.sol
  - hardhat/week-2/solution/test/ConfidentialERC20.ts
  - hardhat/week-2/solution/tsconfig.json
  - hardhat/week-3/starter/hardhat.config.ts
  - hardhat/week-3/starter/package.json
  - hardhat/week-3/starter/contracts/SealedBidAuction.sol
  - hardhat/week-3/starter/test/SealedBidAuction.ts
  - hardhat/week-3/starter/tsconfig.json
  - hardhat/week-3/solution/hardhat.config.ts
  - hardhat/week-3/solution/package.json
  - hardhat/week-3/solution/contracts/SealedBidAuction.sol
  - hardhat/week-3/solution/test/SealedBidAuction.ts
  - hardhat/week-3/solution/tsconfig.json
  - hardhat/week-4/starter/hardhat.config.ts
  - hardhat/week-4/starter/package.json
  - hardhat/week-4/starter/contracts/ConfidentialDApp.sol
  - hardhat/week-4/starter/test/ConfidentialDApp.ts
  - hardhat/week-4/starter/tsconfig.json
  - hardhat/week-4/solution/hardhat.config.ts
  - hardhat/week-4/solution/package.json
  - hardhat/week-4/solution/contracts/ConfidentialDApp.sol
  - hardhat/week-4/solution/test/ConfidentialDApp.ts
  - hardhat/week-4/solution/tsconfig.json
  - hardhat/README.md
autonomous: true
requirements: [REPO-01, REPO-02, REPO-03]

must_haves:
  truths:
    - "Each week has a starter/ directory with a compilable Hardhat project containing TODO stubs"
    - "Each week has a solution/ directory with a compilable Hardhat project containing completed code"
    - "Starter code compiles but has TODO placeholders where students need to implement"
    - "Solution code is the completed implementation matching homework deliverables"
    - "All 8 projects (4 weeks x 2 variants) use fhevm hardhat plugin with mock mode"
    - "All 8 projects can be independently installed and compiled via npx hardhat compile"
  artifacts:
    - path: "hardhat/week-1/starter/hardhat.config.ts"
      provides: "Week 1 starter Hardhat config"
      contains: "fhevm/hardhat"
    - path: "hardhat/week-1/solution/hardhat.config.ts"
      provides: "Week 1 solution Hardhat config"
      contains: "fhevm/hardhat"
    - path: "hardhat/week-2/starter/contracts/ConfidentialERC20.sol"
      provides: "Week 2 starter contract with TODOs"
      contains: "TODO"
    - path: "hardhat/week-2/solution/contracts/ConfidentialERC20.sol"
      provides: "Week 2 completed contract"
      min_lines: 30
    - path: "hardhat/week-3/starter/contracts/SealedBidAuction.sol"
      provides: "Week 3 starter contract with TODOs"
      contains: "TODO"
    - path: "hardhat/week-4/starter/contracts/ConfidentialDApp.sol"
      provides: "Week 4 capstone starter"
      contains: "TODO"
    - path: "hardhat/README.md"
      provides: "Setup instructions for students"
      min_lines: 20
  key_links:
    - from: "hardhat/week-*/starter/hardhat.config.ts"
      to: "fhevm/hardhat"
      via: "import plugin"
      pattern: 'import "fhevm/hardhat"'
    - from: "hardhat/week-*/solution/contracts/*.sol"
      to: "fhevm/lib/TFHE.sol"
      via: "Solidity import"
      pattern: "import.*fhevm"
---

<objective>
Create the Hardhat monorepo with starter code (TODO stubs) and complete solutions for all 4 weeks. Each week is an independent Hardhat project with fhevm plugin. All 8 projects must compile successfully via `npx hardhat compile`.

Purpose: Students get hands-on code scaffolding aligned with homework assignments. Judges see a working code repository.
Output: /hardhat directory with week-1 through week-4, each containing starter/ and solution/ Hardhat projects.
</objective>

<execution_context>
@/Users/0xemrek/.claude/get-shit-done/workflows/execute-plan.md
@/Users/0xemrek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dashboard-landing-page-and-polish/05-CONTEXT.md
@.planning/phases/05-dashboard-landing-page-and-polish/05-RESEARCH.md

@lib/curriculum.ts
@content/homework/homework-1.tsx
@content/homework/homework-2.tsx
@content/homework/homework-3.tsx
@content/homework/homework-4.tsx

<interfaces>
<!-- Key info from curriculum.ts homework specifications -->

Week 1 - Temperature Converter Migration (Beginner):
  Deliverables: Replace uint32 with euint32, accept encrypted input via externalEuint32 + inputProof,
  implement FHE arithmetic for conversion, set ACL permissions with FHE.allowThis and FHE.allow

Week 2 - Confidential ERC-20 Token (Intermediate):
  Deliverables: Implement confidential balances using euint64, encrypted transfer with overflow protection,
  implement allowance system with encrypted amounts, add view functions using reencryption pattern

Week 3 - Sealed-Bid Auction dApp (Advanced):
  Deliverables: Accept encrypted bids via einput, compare encrypted bids using FHE.max,
  implement bid reveal using reencryption, handle auction lifecycle (open/close/reveal)

Week 4 - Capstone: Confidential dApp (Advanced):
  Deliverables: Design and implement a novel confidential dApp, combine encrypted state + access control + reencryption,
  write comprehensive tests covering all FHEVM patterns, document design decisions and trade-offs

Hardhat config pattern (from Context7 fhevm docs):
```typescript
import { HardhatUserConfig } from "hardhat/config";
import "@nomicfoundation/hardhat-toolbox";
import "fhevm/hardhat";
const config: HardhatUserConfig = { solidity: "0.8.24", defaultNetwork: "hardhat" };
export default config;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared boilerplate and README for all 8 Hardhat projects</name>
  <files>
    hardhat/week-1/starter/hardhat.config.ts,
    hardhat/week-1/starter/package.json,
    hardhat/week-1/starter/tsconfig.json,
    hardhat/week-1/solution/hardhat.config.ts,
    hardhat/week-1/solution/package.json,
    hardhat/week-1/solution/tsconfig.json,
    hardhat/week-2/starter/hardhat.config.ts,
    hardhat/week-2/starter/package.json,
    hardhat/week-2/starter/tsconfig.json,
    hardhat/week-2/solution/hardhat.config.ts,
    hardhat/week-2/solution/package.json,
    hardhat/week-2/solution/tsconfig.json,
    hardhat/week-3/starter/hardhat.config.ts,
    hardhat/week-3/starter/package.json,
    hardhat/week-3/starter/tsconfig.json,
    hardhat/week-3/solution/hardhat.config.ts,
    hardhat/week-3/solution/package.json,
    hardhat/week-3/solution/tsconfig.json,
    hardhat/week-4/starter/hardhat.config.ts,
    hardhat/week-4/starter/package.json,
    hardhat/week-4/starter/tsconfig.json,
    hardhat/week-4/solution/hardhat.config.ts,
    hardhat/week-4/solution/package.json,
    hardhat/week-4/solution/tsconfig.json,
    hardhat/README.md
  </files>
  <action>
Create the /hardhat directory structure at project root with all 8 project scaffolds (boilerplate only — no contracts or tests yet).

**Shared pattern for ALL 8 projects:**

Each project directory (`hardhat/week-{1,2,3,4}/{starter,solution}/`) needs:
- `hardhat.config.ts`: Import hardhat-toolbox and fhevm/hardhat, Solidity 0.8.24, defaultNetwork "hardhat"
- `package.json`: name (e.g., "fhe-academy-week-1-starter"), scripts (compile, test, clean), dependencies on hardhat, @nomicfoundation/hardhat-toolbox, fhevm, typescript, ts-node, @types/node
- `tsconfig.json`: Standard TypeScript config for Hardhat (target ES2020, module commonjs, esModuleInterop, strict)
- Create empty `contracts/` and `test/` directories (with .gitkeep if needed)

**hardhat/README.md:**
- Title: "FHE Academy - Hardhat Projects"
- Brief explanation: each week has starter/ and solution/
- Setup instructions: `cd hardhat/week-1/starter && npm install && npx hardhat compile`
- Mock mode explanation: all projects use local Hardhat network with fhevm mock
- Structure overview showing all 4 weeks

IMPORTANT: Use `import "fhevm/hardhat"` (NOT the old separate fhevm-hardhat-plugin package). Use Solidity 0.8.24. Each project is fully independent with its own package.json and node_modules.

Do NOT run npm install — these are template projects students will install themselves.
  </action>
  <verify>
    <automated>ls /Users/0xemrek/Desktop/fheacademy/hardhat/week-{1,2,3,4}/{starter,solution}/hardhat.config.ts && ls /Users/0xemrek/Desktop/fheacademy/hardhat/week-{1,2,3,4}/{starter,solution}/package.json && cat /Users/0xemrek/Desktop/fheacademy/hardhat/README.md | head -5</automated>
  </verify>
  <done>All 8 project directories exist with hardhat.config.ts (fhevm/hardhat plugin), package.json, and tsconfig.json. README.md provides setup instructions. Contracts and tests will be added in Tasks 2 and 3.</done>
</task>

<task type="auto">
  <name>Task 2: Create Week 1-2 contracts and tests (starter + solution)</name>
  <files>
    hardhat/week-1/starter/contracts/TemperatureConverter.sol,
    hardhat/week-1/starter/test/TemperatureConverter.ts,
    hardhat/week-1/solution/contracts/FHETemperatureConverter.sol,
    hardhat/week-1/solution/test/FHETemperatureConverter.ts,
    hardhat/week-2/starter/contracts/ConfidentialERC20.sol,
    hardhat/week-2/starter/test/ConfidentialERC20.ts,
    hardhat/week-2/solution/contracts/ConfidentialERC20.sol,
    hardhat/week-2/solution/test/ConfidentialERC20.ts
  </files>
  <action>
Create contracts and tests for Weeks 1 and 2. Focus on getting correct, compilable Solidity.

**Week 1 — Temperature Converter Migration:**

Starter (`hardhat/week-1/starter/`):
- `contracts/TemperatureConverter.sol`: The PLAINTEXT Solidity contract that students will migrate. Standard contract with:
  - `uint32 public lastCelsius` and `uint32 public lastFahrenheit` state variables
  - `function convert(uint32 celsius) public` that computes F = (C * 9 / 5) + 32 and stores results
  - `function getLastConversion() public view returns (uint32, uint32)` to read last values
  - Comments with TODO markers pointing students to what needs migration:
    ```
    // TODO: Replace uint32 with euint32 for encrypted state variables
    // TODO: Accept einput + bytes inputProof instead of plain uint32
    // TODO: Replace arithmetic with FHE.mul, FHE.div, FHE.add
    // TODO: Add ACL permissions with FHE.allowThis() and FHE.allow()
    ```
- `test/TemperatureConverter.ts`: Basic test file with a test that deploys and calls convert(100), checks result is 212. Include TODO comments for students to add FHEVM test patterns.

Solution (`hardhat/week-1/solution/`):
- `contracts/FHETemperatureConverter.sol`: Complete FHEVM migration:
  - `import "fhevm/lib/TFHE.sol"` and `import "fhevm/config/ZamaFHEVMConfig.sol"`
  - Contract inherits `SepoliaZamaFHEVMConfig` (for config) or uses appropriate config
  - `euint32 private lastCelsius` and `euint32 private lastFahrenheit`
  - `function convert(einput encryptedCelsius, bytes calldata inputProof) public` that:
    - Converts input: `euint32 celsius = TFHE.asEuint32(encryptedCelsius, inputProof)`
    - Computes: `euint32 step1 = TFHE.mul(celsius, 9)` -> `euint32 step2 = TFHE.div(step1, 5)` -> `euint32 fahrenheit = TFHE.add(step2, TFHE.asEuint32(32))`
    - Stores results and sets ACL: `TFHE.allowThis(fahrenheit)`, `TFHE.allow(fahrenheit, msg.sender)`
  - View function with reencryption pattern
- `test/FHETemperatureConverter.ts`: Complete test using fhevm test utilities — deploy, create encrypted input, call convert, verify result via reencryption

**Week 2 — Confidential ERC-20 Token:**

Starter (`hardhat/week-2/starter/`):
- `contracts/ConfidentialERC20.sol`: Contract skeleton with:
  - `import "fhevm/lib/TFHE.sol"` already present
  - State variables declared but mappings use `// TODO: replace uint256 with euint64`
  - `function transfer(address to, /* TODO: accept einput */) public` with TODO body
  - `function balanceOf(/* TODO: add reencryption params */) public view` with TODO body
  - `function approve(address spender, /* TODO */) public` with TODO body
  - `function transferFrom(/* TODO */) public` with TODO body
  - Mint function that creates initial encrypted balance
- `test/ConfidentialERC20.ts`: Test skeleton with describe/it blocks but TODO bodies

Solution (`hardhat/week-2/solution/`):
- `contracts/ConfidentialERC20.sol`: Complete implementation with:
  - `mapping(address => euint64) private balances`
  - `mapping(address => mapping(address => euint64)) private allowances`
  - Encrypted transfer with `TFHE.select` for conditional (balance >= amount check using `TFHE.le`)
  - Allowance system with encrypted amounts
  - balanceOf with reencryption pattern (requires valid signature)
  - Proper ACL permissions on all encrypted values
- `test/ConfidentialERC20.ts`: Complete tests for mint, transfer, approve, transferFrom

IMPORTANT: Use `import "fhevm/hardhat"` (NOT the old separate fhevm-hardhat-plugin package). Use Solidity 0.8.24. Each project is fully independent.

After creating all files, run `npm install && npx hardhat compile` in each of the 4 project directories (week-1/starter, week-1/solution, week-2/starter, week-2/solution) to verify compilation succeeds. Fix any compilation errors before proceeding.
  </action>
  <verify>
    <automated>cd /Users/0xemrek/Desktop/fheacademy/hardhat/week-1/starter && npm install && npx hardhat compile && cd /Users/0xemrek/Desktop/fheacademy/hardhat/week-1/solution && npm install && npx hardhat compile && cd /Users/0xemrek/Desktop/fheacademy/hardhat/week-2/starter && npm install && npx hardhat compile && cd /Users/0xemrek/Desktop/fheacademy/hardhat/week-2/solution && npm install && npx hardhat compile</automated>
  </verify>
  <done>All contracts compile successfully via npx hardhat compile. Starter contracts contain TODO placeholders. Solution contracts contain complete FHEVM implementations. Week 1 and Week 2 each have working starter and solution projects.</done>
</task>

<task type="auto">
  <name>Task 3: Create Week 3-4 contracts and tests (starter + solution)</name>
  <files>
    hardhat/week-3/starter/contracts/SealedBidAuction.sol,
    hardhat/week-3/starter/test/SealedBidAuction.ts,
    hardhat/week-3/solution/contracts/SealedBidAuction.sol,
    hardhat/week-3/solution/test/SealedBidAuction.ts,
    hardhat/week-4/starter/contracts/ConfidentialDApp.sol,
    hardhat/week-4/starter/test/ConfidentialDApp.ts,
    hardhat/week-4/solution/contracts/ConfidentialDApp.sol,
    hardhat/week-4/solution/test/ConfidentialDApp.ts
  </files>
  <action>
Create contracts and tests for Weeks 3 and 4. Focus on getting correct, compilable Solidity.

**Week 3 — Sealed-Bid Auction dApp:**

Starter (`hardhat/week-3/starter/`):
- `contracts/SealedBidAuction.sol`: Contract skeleton with:
  - `import "fhevm/lib/TFHE.sol"` present
  - Auction lifecycle enum: `enum AuctionState { Open, Closed, Revealed }`
  - State: `address public owner`, `AuctionState public state`, `uint256 public endTime`
  - `mapping(address => euint64) private bids` declared
  - `euint64 private highestBid` and `address private highestBidder` declared
  - `function placeBid(/* TODO: accept einput for encrypted bid */) external` with TODO body:
    ```
    // TODO: Convert einput to euint64
    // TODO: Store the encrypted bid
    // TODO: Set ACL permissions
    // TODO: Compare with current highest using TFHE.max
    ```
  - `function closeAuction() external` — simple state transition (onlyOwner)
  - `function revealWinner(/* TODO: reencryption params */) external view` with TODO:
    ```
    // TODO: Implement reencryption to reveal winning bid to authorized caller
    ```
  - Modifiers: `onlyOwner`, `inState(AuctionState)` provided
- `test/SealedBidAuction.ts`: Test skeleton with describe blocks for placing bids, closing auction, revealing winner — TODO bodies

Solution (`hardhat/week-3/solution/`):
- `contracts/SealedBidAuction.sol`: Complete implementation:
  - `placeBid`: Converts einput, stores bid, uses `TFHE.max(highestBid, newBid)` to update highest, uses `TFHE.eq` + `TFHE.select` pattern to conditionally update highestBidder
  - Actually, since we can't directly compare and get a boolean for address selection: use `ebool isHigher = TFHE.gt(newBid, highestBid)` then update highestBid with `TFHE.select(isHigher, newBid, highestBid)`
  - For highestBidder: store alongside and update when bid is higher (this requires a workaround since we can't conditionally assign an address with TFHE.select on addresses — use encrypted bid comparison and track separately)
  - Proper ACL: `TFHE.allowThis(bid)`, `TFHE.allow(bid, msg.sender)`
  - closeAuction: sets state to Closed
  - revealWinner: reencryption pattern for highest bid value
- `test/SealedBidAuction.ts`: Complete tests for bid placement, auction lifecycle, winner reveal

**Week 4 — Capstone: Confidential dApp:**

For the capstone, create a Confidential Voting dApp as the example project (combines all FHEVM patterns learned):

Starter (`hardhat/week-4/starter/`):
- `contracts/ConfidentialDApp.sol`: A confidential voting contract skeleton:
  - Title comment: "Capstone: Confidential Voting dApp"
  - `import "fhevm/lib/TFHE.sol"` present
  - State: proposal names (string[]), encrypted vote tallies (`mapping(uint256 => euint64)`), voter records
  - `function createProposal(string memory name) external` — provided (plaintext, simple)
  - `function castVote(uint256 proposalId, /* TODO: encrypted vote einput */) external` with TODO:
    ```
    // TODO: Convert einput to encrypted vote value
    // TODO: Add vote to proposal tally using TFHE.add
    // TODO: Set ACL permissions
    // TODO: Mark voter as having voted
    ```
  - `function getResults(uint256 proposalId, /* TODO: reencryption params */) external view` with TODO:
    ```
    // TODO: Implement reencryption to reveal vote tally to authorized caller
    ```
  - `function hasVoted(address voter, uint256 proposalId) external view returns (bool)` — provided
- `test/ConfidentialDApp.ts`: Test skeleton with describe blocks for creating proposals, casting votes, getting results — TODO bodies

Solution (`hardhat/week-4/solution/`):
- `contracts/ConfidentialDApp.sol`: Complete implementation:
  - castVote: converts einput to euint64, adds to tally with TFHE.add, sets ACL, marks voter
  - getResults: reencryption pattern for vote tally
  - Access control: only owner can create proposals, any address can vote once per proposal
  - Proper ACL on all encrypted values
- `test/ConfidentialDApp.ts`: Complete tests covering all patterns

IMPORTANT: These contracts should align with what the curriculum lessons teach. Use the same FHEVM import paths and patterns shown in the lesson content. Use Solidity 0.8.24 consistently.

After creating all files, run `npm install && npx hardhat compile` in each of the 4 project directories (week-3/starter, week-3/solution, week-4/starter, week-4/solution) to verify compilation succeeds. Fix any compilation errors before proceeding.
  </action>
  <verify>
    <automated>cd /Users/0xemrek/Desktop/fheacademy/hardhat/week-3/starter && npm install && npx hardhat compile && cd /Users/0xemrek/Desktop/fheacademy/hardhat/week-3/solution && npm install && npx hardhat compile && cd /Users/0xemrek/Desktop/fheacademy/hardhat/week-4/starter && npm install && npx hardhat compile && cd /Users/0xemrek/Desktop/fheacademy/hardhat/week-4/solution && npm install && npx hardhat compile</automated>
  </verify>
  <done>All contracts compile successfully via npx hardhat compile. Starter contracts contain TODO placeholders. Solution contracts contain complete FHEVM implementations. Week 3 and Week 4 each have working starter and solution projects.</done>
</task>

</tasks>

<verification>
1. All 8 Hardhat project directories exist: `hardhat/week-{1,2,3,4}/{starter,solution}/`
2. Each project has: hardhat.config.ts, package.json, tsconfig.json, contracts/*.sol, test/*.ts
3. All hardhat.config.ts files import "fhevm/hardhat" and use Solidity 0.8.24
4. All 8 projects compile successfully via `npx hardhat compile` (after npm install)
5. Starter contracts contain TODO comments guiding students on what to implement
6. Solution contracts contain complete FHEVM implementations
7. Contract content matches homework deliverables from curriculum.ts
8. README.md provides setup instructions for students
9. `grep -r "TODO" hardhat/week-*/starter/contracts/` returns results for all 4 weeks
</verification>

<success_criteria>
- 8 independent Hardhat projects (4 weeks x starter/solution)
- Each project uses fhevm/hardhat plugin with mock mode (defaultNetwork: "hardhat")
- All contracts compile successfully via `npx hardhat compile`
- Starter code has TODO placeholders matching homework deliverables
- Solution code has complete FHEVM implementations
- Projects are self-contained (own package.json, can be installed independently)
- README.md explains structure and setup
- Contracts use Solidity 0.8.24 and import from "fhevm/lib/TFHE.sol"
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard-landing-page-and-polish/05-03-SUMMARY.md`
</output>
