---
phase: 03-full-curriculum-content
plan: 04
type: execute
wave: 4
depends_on: ["03-03"]
files_modified:
  - content/lessons/lesson-3-1.tsx
  - content/lessons/lesson-3-2.tsx
  - content/lessons/lesson-3-3.tsx
  - content/lessons/lesson-3-4.tsx
  - content/lessons/lesson-3-5.tsx
  - app/(academy)/week/[weekId]/lesson/[lessonId]/page.tsx
  - app/(academy)/week/[weekId]/homework/[homeworkSlug]/page.tsx
autonomous: true
requirements:
  - CURR-02
  - CURR-03
  - CURR-04
  - CURR-08
  - HW-04
  - HW-07
  - PLAT-04

must_haves:
  truths:
    - "Lessons 3.1-3.5 all render full content with CodeDiff, Quiz, and InstructorNotes"
    - "Lesson 3.1 uses the v0.9 self-relaying decryption model — NOT Gateway.requestDecryption"
    - "Every Week 3 lesson demonstrates the Migration Mindset via CodeDiff"
    - "Week 3 homework page shows Sealed-Bid Auction dApp spec with rubric"
  artifacts:
    - path: "content/lessons/lesson-3-1.tsx"
      provides: "The Decryption Mechanism lesson content"
      exports: ["Lesson3_1Content", "lesson3_1Meta"]
    - path: "content/lessons/lesson-3-2.tsx"
      provides: "Conditional Logic with FHE.select lesson content"
      exports: ["Lesson3_2Content", "lesson3_2Meta"]
    - path: "content/lessons/lesson-3-3.tsx"
      provides: "On-Chain Randomness lesson content"
      exports: ["Lesson3_3Content", "lesson3_3Meta"]
    - path: "content/lessons/lesson-3-4.tsx"
      provides: "Frontend Integration lesson content"
      exports: ["Lesson3_4Content", "lesson3_4Meta"]
    - path: "content/lessons/lesson-3-5.tsx"
      provides: "Auction and Voting Patterns lesson content"
      exports: ["Lesson3_5Content", "lesson3_5Meta"]
  key_links:
    - from: "app/(academy)/week/[weekId]/lesson/[lessonId]/page.tsx"
      to: "content/lessons/lesson-3-*.tsx"
      via: "lessonRegistry imports"
      pattern: "import.*lesson-3-"
---

<objective>
Author all Week 3 lesson content (lessons 3.1-3.5) and the Week 3 homework page (Sealed-Bid Auction dApp).

Purpose: Complete the third week covering real-world dApp building: decryption mechanism, FHE.select patterns, on-chain randomness, frontend integration, and auction/voting patterns. Week 3 is where students transition from isolated contracts to complete dApps.

Output: 5 lesson content files with full JSX content, all registered in the lesson route handler, and a complete Week 3 homework page with rubric.
</objective>

<execution_context>
@/Users/0xemrek/.claude/get-shit-done/workflows/execute-plan.md
@/Users/0xemrek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-full-curriculum-content/03-03-SUMMARY.md

@content/lessons/lesson-1-4.tsx
@content/fhevm-api-reference.tsx
@content/solidity-to-fhevm-guide.tsx
@lib/curriculum.ts
@app/(academy)/week/[weekId]/lesson/[lessonId]/page.tsx

**Primary content reference:** `/Users/0xemrek/Downloads/FHE_Academy___Complete_Project_Plan.md` — Week 3 section (starting at "WEEK 3: Building Real-World Confidential dApps").

**CRITICAL v0.9 CORRECTION (from STATE.md):** Lesson 3.1 decryption MUST use the v0.9 self-relaying model from `content/fhevm-api-reference.tsx` Section "Self-Relaying Decryption (v0.9)". The project plan document incorrectly references `Gateway.requestDecryption` — this is DEPRECATED. Use `FHE.makePubliclyDecryptable()` + relayer SDK pattern instead.

<interfaces>
Follow the exact same component pattern as lesson-1-4.tsx (see Plan 02 interfaces).

Key component signatures (same as Plans 02-03):
- CodeDiff, CodeBlock, Quiz, CalloutBox, InstructorNotes
- Body text: `text-text-secondary leading-relaxed`
- Inline code: `rounded bg-code-bg px-1.5 py-0.5 text-sm font-mono text-primary`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Week 3 lesson content files (3.1-3.5)</name>
  <files>content/lessons/lesson-3-1.tsx, content/lessons/lesson-3-2.tsx, content/lessons/lesson-3-3.tsx, content/lessons/lesson-3-4.tsx, content/lessons/lesson-3-5.tsx, app/(academy)/week/[weekId]/lesson/[lessonId]/page.tsx</files>
  <action>
Create 5 lesson content files following the established pattern.

**CRITICAL: Lesson 3.1 MUST use v0.9 self-relaying decryption model.** The project plan document references `Gateway.requestDecryption` which is DEPRECATED in v0.9. Instead, use `FHE.makePubliclyDecryptable()` and the relayer SDK pattern from `content/fhevm-api-reference.tsx`. Read the "Self-Relaying Decryption (v0.9)" section of that file BEFORE writing lesson 3.1.

**CRITICAL: Cross-reference `content/fhevm-api-reference.tsx` for ALL code examples.** Pay special attention to the randomness section for lesson 3.3 and the voting contract example for lesson 3.5.

**Lesson 3.1 — The Decryption Mechanism** (hands-on):
- learningObjective: "Understand the v0.9 self-relaying decryption mechanism and know when to decrypt vs keep data encrypted."
- Content: Data stays encrypted by default, the v0.9 decryption model (FHE.makePubliclyDecryptable -> relayer SDK -> on-chain plaintext storage), when to decrypt (user's own balance, auction winner, vote totals) vs when NOT to (during computation, intermediate values), mock mode (synchronous) vs mainnet (async)
- **IMPORTANT:** Use the v0.9 pattern: `FHE.makePubliclyDecryptable(handle)` marks a ciphertext for decryption. The relayer SDK picks it up and submits the decrypted value on-chain. Show the contract pattern AND the frontend/relayer integration.
- CodeDiff: Standard view function returning plaintext vs FHEVM decryption request pattern (showing FHE.makePubliclyDecryptable + callback storage)
- CodeDiff: Standard getter returning value vs FHEVM getter that returns the stored decrypted plaintext
- CodeBlock: Relayer SDK setup and usage (TypeScript, from fhevm-api-reference.tsx)
- CalloutBox (warning): Decryption is a conscious design decision — once decrypted, data is public forever
- CalloutBox (info): In mock mode, decryption is synchronous. On mainnet, it takes a few blocks.
- Quiz: (1) What does FHE.makePubliclyDecryptable do? (2) When should you NOT decrypt data? (3) Code-reading: given a contract, identify what happens when decryption completes
- InstructorNotes: This replaces the old Gateway pattern. Students may find online resources with Gateway.requestDecryption — tell them that is deprecated in v0.9.

**Lesson 3.2 — Conditional Logic with FHE.select** (hands-on):
- learningObjective: "Implement complex business logic using encrypted branching with FHE.select, including nested conditions and multi-path selection."
- Content: Deep dive into FHE.select(condition, ifTrue, ifFalse), min/max clamping pattern, conditional transfer (only if sufficient balance), tiered pricing (chained FHE.select for multiple ranges), performance note (both branches always computed)
- CodeDiff: Standard Solidity tiered pricing with if/else if/else vs FHEVM chained FHE.select
- CodeDiff: Standard require() guard vs FHE.select guard (conditional execution without reverting)
- CodeBlock: Complete conditional transfer pattern
- CalloutBox (tip): Think of FHE.select as a multiplexer, not an if/else — both sides are always evaluated
- Quiz: (1) Why are both branches of FHE.select always computed? (2) Code-reading: given nested FHE.select, trace the output, (3) How does FHE.select replace require() guards?
- InstructorNotes: This is the most important programming pattern shift. Students coming from Solidity instinctively reach for if/else.

**Lesson 3.3 — On-Chain Randomness** (hands-on):
- learningObjective: "Generate encrypted random numbers on-chain using FHE and understand why FHE randomness is superior to existing solutions."
- Content: FHE.randEuint8/16/32/64 functions, why this matters (random number is encrypted — nobody can see or predict it), comparison with Chainlink VRF (reveals the number vs keeps it secret), use cases (card dealing, lottery, hidden game state), bounded randomness with FHE.rem
- CodeDiff: Standard Solidity randomness (blockhash-based, predictable) vs FHEVM encrypted randomness (FHE.randEuint32)
- CodeDiff: Chainlink VRF pattern (public random) vs FHE randomness pattern (encrypted random)
- CodeBlock: Simple encrypted dice game contract
- CalloutBox (tip): FHE randomness is quantum-resistant — no amount of computational power can predict or observe the random value
- Quiz: (1) Why is FHE randomness better than blockhash? (2) How do you generate a random number between 1 and 6? (FHE.rem), (3) What does Chainlink VRF reveal that FHE randomness does not?
- InstructorNotes: Students love this lesson — interactive demo with encrypted dice

**Lesson 3.4 — Frontend Integration** (hands-on):
- learningObjective: "Build a React frontend that creates encrypted inputs, sends transactions to FHEVM contracts, and decrypts results for display."
- Content: Setting up fhevm-react-template, relayer-sdk for encrypted input creation, complete flow (connect wallet -> create encrypted input -> send tx -> read result -> decrypt -> display), building UI for FHECounter, error handling (permission denied on decrypt)
- CodeDiff: Standard ethers.js contract call vs FHEVM encrypted input creation + contract call
- CodeDiff: Standard result reading vs FHEVM decryption + display pattern
- CodeBlock: Complete React component for interacting with FHECounter (TypeScript)
- CalloutBox (info): The SDK handles all FHE key management — you just provide plaintext values and get encrypted inputs back
- CalloutBox (warning): If a user does not have ACL permission, decryption will fail silently — always check permissions first
- Quiz: (1) What does the relayer SDK do on the client side? (2) Code-reading: given a React component, identify where encryption happens, (3) What happens if you try to decrypt without ACL permission?
- InstructorNotes: This is where the full stack comes together. Run through the complete flow step by step.

**Lesson 3.5 — Design Patterns: Auction and Voting** (hands-on):
- learningObjective: "Design and implement sealed-bid auction and private voting patterns using FHEVM encrypted operations and ACL."
- Content: Sealed-bid auction (submit encrypted bids, compare using FHE.gt + FHE.select, reveal only winner), private voting (encrypted votes accumulated with FHE.add, decrypt only total), difference between hiding data and hiding computation
- CodeDiff: Standard open auction (public bids) vs sealed-bid auction (encrypted bids with FHE.gt comparison)
- CodeDiff: Standard public voting vs private voting with encrypted tallying
- CodeBlock: Complete sealed-bid auction contract (reference the voting contract example from fhevm-api-reference.tsx)
- CalloutBox (tip): Losing bids remain encrypted forever — true privacy
- CalloutBox (info): Private voting prevents vote buying/coercion — nobody can prove how they voted
- Quiz: (1) How does the auction contract find the highest bid without seeing any amounts? (2) Why does private voting prevent vote buying? (3) Code-reading: given the auction contract, what happens to losing bids?
- InstructorNotes: These two patterns demonstrate the real power of FHE. Students should understand that these are IMPOSSIBLE with ZKP alone.

After creating all 5 files, add imports and registry entries to the lesson route handler.
  </action>
  <verify>
    <automated>cd /Users/0xemrek/Desktop/fheacademy && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>All 5 Week 3 lesson content files created. Lesson 3.1 uses v0.9 self-relaying decryption (NOT Gateway). All registered in lesson route handler. TypeScript compiles. No deprecated FHEVM patterns.</done>
</task>

<task type="auto">
  <name>Task 2: Author Week 3 homework page (Sealed-Bid Auction dApp)</name>
  <files>app/(academy)/week/[weekId]/homework/[homeworkSlug]/page.tsx</files>
  <action>
Add Week 3 homework content to the homework page. The Sealed-Bid Auction dApp homework should include:

**1. Header:** "Homework: Sealed-Bid Auction dApp", "Week 3", difficulty badge (advanced — red/error)

**2. Overview:** Students build a complete sealed-bid auction with FHEVM smart contract AND React frontend. This is the most ambitious homework yet — combining contract development with frontend integration.

**3. Learning Objectives:** Demonstrate mastery of encrypted comparisons, decryption flow, ACL for multi-party access, and full-stack dApp development.

**4. Requirements** (from project plan):
Smart contract requirements:
- `placeBid()`: accept encrypted bid amount via externalEuint64 + inputProof
- `endAuction()`: compare all bids using FHE.gt + FHE.select to determine winner (owner only)
- `revealWinner()`: decrypt winning bid + address using v0.9 decryption model
- Losing bids remain encrypted forever
- Proper ACL: bidders can verify their own bids, only contract can compare

Frontend requirements:
- Connect wallet
- Input bid amount, encrypt via SDK, submit transaction
- View auction status (open/closed)
- Reveal winner when auction ends

Testing requirements:
- At least 3 test scenarios covering the full auction lifecycle

**5. Transformation Direction via CodeDiff:** Show standard open auction bid function vs sealed-bid auction function signature and bid storage pattern. NOT the full comparison logic.

**6. Getting Started:** Link to Lesson 1.3 for setup, Lesson 3.4 for frontend integration, Lesson 3.5 for auction pattern reference.

**7. Grading Rubric** (from project plan):
Contract Logic (25%), Bid Privacy (20%), Decryption Flow (20%), Frontend (20%), Documentation (15%)

**8. Submission Guidelines:** GitHub repo with contract + frontend + tests + README.
  </action>
  <verify>
    <automated>cd /Users/0xemrek/Desktop/fheacademy && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Week 3 homework page renders with full spec including contract and frontend requirements, rubric, and transformation direction. Build succeeds.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. All 5 Week 3 lessons render full content
3. Lesson 3.1 uses FHE.makePubliclyDecryptable — NOT Gateway.requestDecryption
4. Each lesson has CodeDiff, 2-3 Quiz questions, InstructorNotes
5. No deprecated FHEVM patterns (grep for TFHE., Gateway., einput, ebytes, SepoliaConfig, decryptionOracle)
6. Week 3 homework renders with rubric
</verification>

<success_criteria>
- 5 new lesson content files created and registered (3.1-3.5)
- Lesson 3.1 correctly uses v0.9 self-relaying decryption model
- All lessons follow Migration Mindset with CodeDiff
- Week 3 homework has rubric with contract + frontend requirements
- Zero deprecated FHEVM patterns
- Full build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/03-full-curriculum-content/03-04-SUMMARY.md`
</output>
